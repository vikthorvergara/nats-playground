version: '3.8'

services:
  nats:
    image: nats:latest
    ports:
      - "4222:4222"
      - "8222:8222"
    volumes:
      - ./nats-config:/config
    command: "--http_port 8222 --js --tls --tlscert=/config/certs/server-cert.pem --tlskey=/config/certs/server-key.pem"
